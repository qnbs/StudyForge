# Consciousness Stream

Persistent agent and team learnings for StudyForge. Append dated entries; do not delete historical decisions without explicit team agreement.

## Entscheidungen

2026-05-22 — Graphify solo-dev — Nur `graph.json` + `GRAPH_REPORT.md` auf GitHub; kein cache/memory/wiki/history. Kein watch/hooks auf Low-End. Siehe `docs/GRAPHIFY.md`.

## Offene Fragen

<!-- Format: YYYY-MM-DD — Question — Context -->

## Agent-Learnings

2026-05-22 — Hybrid RAG + model presets shipped — `queryRAG` → `queryRAGHybrid`; `ensureModelForRef` in chat/agent test; removed `@google/genai`/`dotenv`; rule `.cursor/rules/203-hybrid-search.mdc`.
